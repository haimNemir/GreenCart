
{
  "description": "!!!! - In this file we define the dependencies (including typescript) we need for our backend app. And we installing those dependencies using npm install command in the Dockerfile",



  "name": "greencart-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "4.18.2",
    "mongodb": "6.3.0"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "20.10.0",
    "typescript": "5.3.2"
  }
}
